# Фикстура нарочно в CRLF - git не должен её нормализовать,
# иначе тест на CRLF проверял бы пустоту.
# -eol нужен отдельно: в корневом .gitattributes стоит eol=lf, а он сам по
# себе включает нормализацию, даже когда text снят.
crlf.lst -text -eol
